{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "MongoDB (Windows)",
      "type": "shell",
      "command": "mongod -port 3000 -dbpath .\\db"
    },
    {
      "label": "MongoDB (Mac/Linux)",
      "type": "shell",
      "command": "mongod -port 3000 -dbpath ./db"
    }
  ]
}